reference

# Rust build artifacts
target/

# Debug symbols
*.pdb

# Runtime SQLite database files (default path is now target/data/harness.db)
harness.db
harness.db-shm
harness.db-wal
harness.db-journal

# Harness scratch directories (never commit)
.scratch/
target/scratch/

# Verification auto-generated acceptance directories (committed intentionally)
# verification/system-accepted-*/ are committed via release process
