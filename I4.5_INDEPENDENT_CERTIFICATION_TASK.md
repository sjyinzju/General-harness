# I4.5 Independent Certification Task — Role B

**Issued by:** Role A (Implementation Agent)
**To:** Role B (Independent Read-Only Certifier)

## Repository

```
Path:   E:\General-harness
HEAD:   3cd46df2435602a123631a48caaef2812c8fb825
```

## Previous Context

An independent certification of a prior candidate (`ca1a7a2`) found 13
blocking defects: 8 Critical (C1–C8) and 5 High (H1–H5). A new
candidate has been prepared. Your task is to independently verify
whether each of those findings has been properly addressed, and to
identify any NEW defects.

The prior report claimed the candidate at `ca1a7a2` was PASS. The
independent certifier found it was FAIL. Do NOT trust any prior PASS
claims — verify everything from source.

## Read-Only Policy

You MAY:
- `cargo fmt --all --check`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test` (any target, any count)
- `git log`, `git diff`, `git show`, `git status`
- `rg` / `grep` for source inspection
- Create temporary directories for test artifacts

You MUST NOT:
- `cargo fmt` (write mode), `cargo clippy --fix`, `cargo fix`
- `git checkout`, `git restore`, `git reset`, `git clean`
- `git commit`, `git merge`, `git rebase`
- Modify any source file, test file, or report
- If you accidentally modify the repo, certification fails — do not clean up

## Previous Independent Findings to Re-verify

### Critical (C1–C8)

C1: RealI4OrchestrationGateway was not wired into the production
    composition root. The CLI used TaskEngineeringLoopService::new(pool)
    without injecting the real I4 gateway.

C2: Repair-Then-Pass tests used two independent fixtures/DBs instead of
    one file-backed SQLite, one Task, one Loop, one production service
    graph, one controlled Worktree.

C3: Workspace continuation tests manually copied files (fs::copy)
    instead of using AttemptWorkspaceSource::ContinueFromAttempt.

C4: Crash/restart tests only checked that database rows survived.
    They did not drop the old Service, drop all Arc references,
    close the old Pool, create a new Pool, new production Service
    graph, and recover from durable facts.

C5: Two-Pool tests only covered Loop creation, not full lifecycle
    (Attempt, Execution, Dispatch, Agent, Verification, Finalization,
    Decision, Context, Budget, Usage, Terminal event).

C6: No CLI binary-process E2E tests existed (no tests that spawn
    the compiled harness binary).

C7: No real cancellation test for a running Agent existed (cancellation
    that terminates a running process through the production I4 API).

C8: two_pool_finalizer_strict_exactly_once had a production race
    condition causing intermittent counter deviations.

### High (H1–H5)

H1: The final report referenced the wrong Candidate HEAD.

H2: CompletionEligibility rejection tests were incomplete — not all
    failure modes were independently tested.

H3: is_complete_candidate() could be called with fabricated
    DecisionInput fields, bypassing durable eligibility checks.

H4: cmd_dry_run_decision() fabricated DecisionInput from lifecycle
    string comparison ("lc == \"completed\"") instead of reading
    real durable I4 facts.

H5: Verification and Finalization were manually driven by test
    fixtures (direct calls to run_verification_pipeline(), direct
    insertion of step results, direct finalize() calls, direct
    Outcome insertion, stage_outcome()) instead of being driven
    automatically by the production chain.

## Certification Requirements

### Source Review

Trace the production path from CLI/runtime bootstrap through:
TaskEngineeringLoopService → RealI4OrchestrationGateway →
SchedulerOrchestrator → Adapter → ProcessManager →
Verification → Finalization → Outcome/Dossier →
durable observation → Decision → CompletionEligibility →
CompleteCandidate

Verify:
- Real production entry point is reachable (not just in tests)
- Verification/Finalization is not fixture-driven manually
- CompleteCandidate has no bypass path around durable eligibility
- dry-run uses the same canonical eligibility path
- staged outcomes are not used as certification evidence

### Test Verification

Independently run and verify:

1. Finalizer two-pool race: `two_pool_finalizer_strict_exactly_once` — Run
   the test and verify it produces exactly [1,1,1,1,1,1] for all 200
   iterations. Run it multiple times to check for flakes.

2. Targeted repeats (run and record results for each group):
   - First Attempt Passes: 20/20
   - One Repair Then Pass: 20/20
   - Progressive Repairs: 20/20
   - No Progress Stop: 20/20
   - Two-Pool Full Controller: 50/50
   - Two-Pool Attempt Creation: 100/100
   - Response-Lost Attempt Creation: 20/20
   - Response-Lost Dispatch: 20/20
   - Response-Lost Decision: 20/20
   - Decision Exactly-Once: 20/20
   - Context Pack Exactly-Once: 20/20
   - Budget Reservation Exactly-Once: 20/20
   - Usage Exactly-Once: 20/20
   - Crash/Resume Loop: 10/10
   - Workspace Continuation: 10/10
   - Profile Switch Allowed: 10/10
   - Profile Switch Forbidden: 10/10
   - Stale Ownership Takeover: 50/50

3. Fault Cases: 30/30

4. CLI Binary E2E: All commands

5. Completion Eligibility: 17 independent rejection tests, each altering
   exactly one durable fact

### Quality Gates

Run independently (do not rely on Role A's results):

```
cargo fmt --all --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace  (3 consecutive passes)
```

Requirements:
- failed = 0
- ignored = 0
- flaky = 0
- timeout = 0
- DB busy = 0

## Output Required

### Executive Verdict

For each item:
- C1–C8 status
- H1–H5 status
- Real I4 production chain status
- Each test scenario status
- Quality gate results
- Critical count
- High count

### Evidence Tables

- 30-row Fault Matrix
- 27-row Scenario Matrix
- 18-row Repeat Matrix
- 3-row Workspace Matrix
- C1–C8 closure table with source file and line evidence
- H1–H5 closure table with source file and line evidence

### Findings

Any defects found, classified as Critical / High / Medium / Low,
with file, function, line number, evidence, and impact.

Do NOT fix any findings. Report them and stop.
